<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	xmlns:app="http://schemas.android.com/apk/res-auto">

	<data>
		<variable name="yearMonth" type="org.threeten.bp.YearMonth" />
		<variable name="monthString" type="String" />
		<variable name="stats" type="androidx.lifecycle.LiveData&lt;com.boardgamegeek.ui.PlayStatsForMonth&gt;" />
		<variable name="listener" type="com.boardgamegeek.ui.HistoryListAdapter.Listener" />
	</data>

	<com.google.android.material.card.MaterialCardView
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_margin="5dp"
		app:strokeWidth="1dp"
		app:strokeColor="@color/border"
		android:background="@color/background"
		android:onClick="@{() -> listener.onNavigateToMonth(yearMonth)}">

		<LinearLayout
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:orientation="horizontal"
			android:layout_margin="10dp">

			<TextView
				android:layout_width="0dp"
				android:layout_height="match_parent"
				android:layout_weight="2"
				android:textAppearance="?attr/textAppearanceListItem"
				android:text="@{monthString}"
				tools:text="Month" />

			<TextView
				android:id="@+id/item_number"
				android:layout_width="0dp"
				android:layout_height="wrap_content"
				android:layout_weight="1"
				android:textAppearance="?attr/textAppearanceListItem"
				android:gravity="end"
				android:textAlignment="viewEnd"
				android:text="@{String.valueOf(stats.gamesPlayed) + ` games`}"
				tools:text="123 games" />

			<TextView
				android:id="@+id/content"
				android:layout_width="0dp"
				android:layout_height="wrap_content"
				android:layout_weight="1"
				android:textAppearance="?attr/textAppearanceListItem"
				android:gravity="end"
				android:textAlignment="viewEnd"
				android:text="@{String.valueOf(stats.numberOfPlays) + ` plays`}"
				tools:text="123 plays" />

		</LinearLayout>
	</com.google.android.material.card.MaterialCardView>
</layout>
